<Window x:Class="MigracaoAD.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Migração AD – Assistente" Height="720" Width="1180"
        WindowStartupLocation="CenterScreen" Background="{DynamicResource AppBg}">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="260"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Header -->
    <Border Grid.ColumnSpan="2" Background="{DynamicResource HeaderBg}" Padding="20,16" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1">
      <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
        <TextBlock Text="🔄 " FontSize="24"/>
        <TextBlock Text="Migração AD" FontSize="20" FontWeight="Bold" Margin="4,0,0,0"/>
        <TextBlock Text="  •  " FontSize="16" Foreground="{DynamicResource TextSecondary}" Margin="8,0"/>
        <TextBlock Text="Assistente guiado" FontSize="16" Foreground="{DynamicResource TextSecondary}"/>
      </StackPanel>
    </Border>

    <!-- Steps -->
    <Border Grid.Row="1" Background="{DynamicResource SideBg}" Padding="20" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,1,0">
      <StackPanel>
        <TextBlock Text="Etapas" FontWeight="Bold" FontSize="16" Margin="0,0,0,12"/>
        <ListBox x:Name="StepsList" SelectedIndex="0">
          <ListBoxItem Content="1. Bem-vindo"/>
          <ListBoxItem Content="2. Ambiente &amp; Credenciais"/>
          <ListBoxItem Content="3. Teste de Agentes"/>
          <ListBoxItem Content="4. Conectividade"/>
          <ListBoxItem Content="5. Modo de Migração"/>
          <ListBoxItem Content="6. Arquivos (Espelho)"/>
          <ListBoxItem Content="7. Resumo &amp; Executar"/>
        </ListBox>
      </StackPanel>
    </Border>

    <!-- Content -->
    <Frame x:Name="ContentFrame" Grid.Row="1" Grid.Column="1" Margin="24" NavigationUIVisibility="Hidden"/>

    <!-- Footer -->
    <Border Grid.Row="2" Grid.ColumnSpan="2" Background="{DynamicResource FooterBg}" Padding="20,16" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,1,0,0">
      <DockPanel>
        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
          <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
            <TextBlock Text="{Binding Source={StaticResource Branding}, Path=Copyright}" Foreground="{DynamicResource TextSecondary}" FontSize="12"/>
            <TextBlock Text=" • Desenvolvido com " Foreground="{DynamicResource TextSecondary}" FontSize="12"/>
            <TextBlock Text="❤" Foreground="#EF4444" FontSize="12"/>
            <TextBlock Text=" e muito " Foreground="{DynamicResource TextSecondary}" FontSize="12"/>
            <TextBlock Text="💪" FontSize="12"/>
            <TextBlock Text=" por " Foreground="{DynamicResource TextSecondary}" FontSize="12"/>
            <TextBlock Text="{Binding Source={StaticResource Branding}, Path=DeveloperName}" Foreground="{DynamicResource TextPrimary}" FontWeight="SemiBold" FontSize="12"/>
          </StackPanel>
          <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
            <TextBlock Margin="0,0,8,0" FontSize="12">
              <Hyperlink NavigateUri="{Binding Source={StaticResource Branding}, Path=LinkedIn}" RequestNavigate="Link_RequestNavigate">LinkedIn</Hyperlink>
            </TextBlock>
            <TextBlock Margin="0,0,8,0" FontSize="12">
              <Hyperlink NavigateUri="{Binding Source={StaticResource Branding}, Path=GitHub}" RequestNavigate="Link_RequestNavigate">GitHub</Hyperlink>
            </TextBlock>
            <TextBlock Margin="0,0,8,0" FontSize="12">
              <Hyperlink NavigateUri="{Binding Source={StaticResource Branding}, Path=Instagram}" RequestNavigate="Link_RequestNavigate">Instagram</Hyperlink>
            </TextBlock>
            <TextBlock FontSize="12">
              <Hyperlink NavigateUri="{Binding Source={StaticResource Branding}, Path=Email}" RequestNavigate="Link_RequestNavigate">Email</Hyperlink>
            </TextBlock>
          </StackPanel>
          <TextBlock Text="{Binding Source={StaticResource Branding}, Path=Tagline}" Foreground="{DynamicResource TextMuted}" FontSize="11" FontStyle="Italic"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right" VerticalAlignment="Center">
          <Button x:Name="BackBtn" Content="← Voltar" Width="120" Click="BackBtn_Click" Margin="4"/>
          <Button x:Name="NextBtn" Content="Avançar →" Width="120" Click="NextBtn_Click" Margin="4"/>
          <Button x:Name="RunBtn" Content="▶ Executar" Width="130" Click="RunBtn_Click" Margin="4"/>
        </StackPanel>
      </DockPanel>
    </Border>
  </Grid>
</Window>

